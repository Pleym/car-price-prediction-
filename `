import tensorflow as tf
from datasets import load_dataset

ds = load_dataset("VarunKumarGupta2003/Car-Price-Dataset")
ds =ds.remove_columns(["Model"])

tf_dataset = model.prepare_tf_dataset(
        ds,
        batch_size = 32,
        shuffle = true
)

train_ds, test_ds = build.as_dataset(split='test+train[:74%]')

x_train = train_ds.drop("Price",axis=1)
y_train = train_ds["Price"]
x_test = test_ds.drop("Price",axis=1)
test_ds = test_ds["Price"]


